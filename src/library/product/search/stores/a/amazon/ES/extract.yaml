extends: ../shared
fields:
  - name: aggregateRating2
    singleValue: false
    xpath: //div[contains(@class,"_multi-card-creative-desktop_Rating_container")]/i | //span[contains(@aria-label,"estrellas")]/@aria-label
    regExp: ^([^\s]+)
  - name: ratingCount
    singleValue: false
    xpath: //span[contains(@aria-label,"estrellas")]/following-sibling::span[1]
  - name: reviewCount
    singleValue: false
    xpath: //span[contains(@aria-label,"estrellas")]/following-sibling::span[1]
  - name: subscribe
    xpath: //*[contains(@dir,"auto") and contains(text(), "uscríbete")]/text()
    # used in transform.
  - name: sponsorReplace
    defaultValue: '.|,'
  - name: domain
    defaultValue: 'amazon.es'