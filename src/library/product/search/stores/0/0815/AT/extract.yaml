singleRecord: false
regionsSelector: null
recordSelector: null
recordXPath: //div[contains(@class,"cms-listing-col")]
fields:
  - name: searchUrl
    xpath: //div[@class="product-image-inner"]//@searchUrl
  - name: productUrl
    description: The URL for the product details page
    xpath: //div[@class="product-image-inner"]/a/@href
  - name: thumbnail
    description: The URL for the product thumbnail image
    xpath: //div[@class="product-image-inner"]//img/@data-src
  - name: name
    description: The product name
    xpath: //div[@class="product-info"]/a/@title
  - name: aggregateRatingText
    singleValue: true
    description: The stated aggregate rating
  - name: ratingCount
    singleValue: true
    description: The number of ratings
  - name: reviewCount
    singleValue: true
    description: The number of reviews
  - name: price
    description: The offered price for the product
    xpath: //div[@class="custom-price"]//div[@class="custom-price-style "]
    regExp: (\d+)(.)(\s)(\d+)
    regExpReplace: $1.$4
  - name: rank
    singleValue: true
    description: search/listing rank
    type: NUMBER    
  - name: rankOrganic
    singleValue: true
    description: The position (rank) within the list excluding sponsored/paid results
    type: NUMBER 
  - name: sponsored
    singleValue: true
    description: Is sponsored/paid
    type: BOOLEAN    

#  Should these be source specific formulas or in the extractor? 
  - name: id
    description: Unique id (within this extractor)
    xpath: //div[@class="product-id-value"]
    regExp: (\d+)
  - name: nameExtended
    singleValue: true
    description: The product name, prefixing "${brand} - " if not included in "name" and appending " - ${variantType}" if necessary (fixed per extractor)
  - name: aggregateRating
    singleValue: true
    description: The stated aggregate rating normalized to be out of 5
    type: NUMBER 
