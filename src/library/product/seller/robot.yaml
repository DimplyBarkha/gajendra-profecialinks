proxy:
  zone: ZUS
  type: DATA_CENTER
policy:
  numberRetries: 3
  priority: MEDIUM
  retryDelay: 60
  backoffPolicy: EXPONENTIAL
  retryWithResidentialProxyAfter: 99
honorRobots: false
schema: product/seller
parameters:
  - domain
  - country
  - store
  - zipcode
entryPoint: product/seller
pathTemplate: product/${store[0:1]}/${store}/${country}/seller
authentication: navigation/auth/action