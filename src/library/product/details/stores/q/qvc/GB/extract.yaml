singleRecord: true
regionsSelector: null
recordSelector: null
recordXPath: null
fields:
  - name: image
    singleValue: false
    # xpath: //div[@aria-label='Video player']/@poster | //div[contains(@class,'easyzoom--with-thumbnails')]//@src | //div[@id='ii_image']
    xpath: //div[@id='ii_image'][last()]
    description: URL of the main product image
  - name: imageAlt
    singleValue: true
    xpath: //div[contains(@class,'easyzoom--with-thumbnails')]//@alt
    description: Alt text of the main product image
  - name: alternateImages
    singleValue: false
    xpath: //a[contains(@class,'thumbcell') and not(contains(@class,'skuThumb'))][position()>1]/@href
    description: Other product images (not in manufacturer description)
  - name: category
    singleValue: false
    xpath: //ol[contains(@class,'breadcrumb pageBreadcrumb')]/li[position()>1]
    description: The categories (e.g. from breadcrumbs)
  - name: nameExtended
    singleValue: false
    xpath: //div[contains(@class,'pdShortDesc')]//h1
    description: The product name, prefixing "${brand} - " if not included in "name"
      and appending " - ${variantType}" if necessary (fixed per feed)
  - name: listPrice
    singleValue: false
    xpath: //span[contains(@class,'price') and contains(@class,'strikeout')]
    description: The stated undiscounted price for the product
  - name: price
    singleValue: true
    # xpath: //span[contains(@class,'productDetailPrice')] | //span[@class='price priceSm'] | //span[@class='price']
    xpath: //div[@id='ii_price'][last()]
    description: The offered price for the selected product variant - if a user
      clicks add to basket this is the price they would be charged
  - name: availabilityText
    # xpath: //div[contains(@class,'buyBoxAvailibility')]//p[contains(@style,'block')]
    xpath: //div[@id='ii_available'][last()]
    singleValue: true
    description: Availability as described on page for the selected variant. If
      there is no availability but the product can be added to cart, this field
      should be "In Stock"
    defaultValue: Out of Stock
  - name: description
    singleValue: false
    manualSelector: div[class*='accLongDesc'] div[class*='accordionBox']
    description: Product description supplied by website - may be concatenanted from
      multiple places; specifically not the manufacturer description
  - name: descriptionBullets
    singleValue: false
    xpath: count(//div[contains(@class,'accLongDesc')]//div[@class='accordionContent']//li[not(.//a/@href[contains(.,'.pdf')])])
    description: Number of ul > li tags in the description
  - name: brandText
    singleValue: false
    xpath: //script[contains(@class,'utagData')]
    description: Product brand shown - this may actually NOT be just a brand name,
      e.g. "Visit the Gillette Store"
  - name: weightNet
    singleValue: false
    xpath: //div[@class='accordionText']//li[contains(.,'Weight')] | //div[@class='accordionText']//p[contains(.,'Weight')]
    description: Net weight as described on the page
  - name: sku
    singleValue: true
    xpath: //div[@id='ii_sku'][last()]
    description: The store-specific product identifier
  - name: variantId
    singleValue: true
    xpath: //div[@id='ii_sku'][last()]
    description: Identifier for a variant from a store where variants share the same sku
  - name: ratingCount
    singleValue: false
    xpath: //div[contains(@class,'bv_numReviews_text')]
    description: The number of ratings
  - name: aggregateRating
    singleValue: false
    xpath: //div[contains(@class,'bv_avgRating_component_container')]
    description: The stated aggregate rating normalized to be out of 5
  - name: variantCount
    singleValue: false
    xpath: count(//ul[contains(@class,'buyBoxColorList')]//li)
    defaultValue: 1
    description: Number of variants listed
  - name: color
    singleValue: false
    xpath: //div[@id='ii_color'][last()]
    description: A textual description of the colour
  - name: colorCode
    singleValue: true
    xpath: //div[@id='ii_colorCode'][last()]
    description: A code description of the color (e.g. RGB hex)
  - name: manufacturerDescription
    singleValue: false
    xpath: //h2[contains(.,'About')]/following-sibling::div
    description: The extended description of the product supplied by the lister
  - name: videos
    singleValue: false
    xpath: //div[contains(@id,'ii_video')]
    description: URLs of videos on page
  - name: name
    singleValue: false
    xpath: //div[contains(@class,'pdShortDesc')]//h1
    description: The product name
  - name: coupon
    singleValue: false
    xpath: //div[contains(@id,'trustBanner')]//a[contains(.,'Code')]
    description: Text of any coupon offer
  - name: specifications
    singleValue: false
    xpath: //div[contains(@class,'accordion')]//h2[contains(.,'Technical')]/following-sibling::div//div[@class="accordionText"]/*
    description: The physical dimensions of the product including packaging.
  # - name: warranty
  #   singleValue: true
  #   description: Guarantee associated with the product
  - name: variantInformation
    singleValue: false
    xpath: //div[@id='ii_color'][last()]
    description: colour / scent / flavour / other
  - name: firstVariant
    singleValue: true
    xpath: //div[@id='ii_sku']
    description: Unique identifier for a collection of variants. For a subset of
      core feeds (43) we have a column "first_variant" that is populated with a
      common code to link all variants for a product. method 1 (Preferred) = use
      common tag (Could be parent sku) that is same value for all variants of a
      product. method 2 = first_variant method technique, Identify the first
      varient in the sequence and use this rpc to populate "first_variant" for
      all variants of a product, this rpc becomes the common link between other
      variants. method 3 = apply some logic to determine link between variants,
      this will need analysis
  - name: variants
    singleValue: false
    description: null
    xpath: //div[@id='ii_variants']
  - name: additionalDescBulletInfo
    singleValue: false
    xpath: //div[contains(@class,'accLongDesc')]//div[@class='accordionContent']//li[not(.//a/@href[contains(.,'.pdf')])]
    description: null
  - name: technicalInformationPdfPresent
    singleValue: false
    xpath: //div[contains(@class,'accLongDesc')]//div[@class='accordionContent']//li//a/@href[contains(.,'.pdf')]
    description: If there is for example a manual in pdf format on the website. ['Yes','No']
    example: https://www.amazon.de/Hasbro-Spiele-44220100-Mastermind-Kinderspiel/dp/B0007OHUG6
    defaultValue: No
  - name: inTheBoxUrl
    singleValue: false
    description: Must include image url to show the accessories/tools which come with the product
  - name: inTheBoxText
    singleValue: false
    xpath: //*[contains(text(), "in the box")]//following-sibling::ul//li|//*[contains(text(), "in the box")]//following-sibling::ul//li|//*[contains(text(), "Contains:")]//following-sibling::ul//li
    description: Must include text to show the accessories/tools which come with the product
  - name: hasComparisonTable
    singleValue: true
    description: Compare the range table or matrix must be present for all vacuum cleaners & air treatment. This is not required for personal care or lighting. As a minimum the comparison table will compare against 1 additional Dyson product.
  - name: unInterruptedPDP
    singleValue: false
    xpath: //section[@data-cs-override-id="PRODUCT_CAROUSEL"]//div[@data-component-type="PRODUCT_CAROUSEL"]//img/@title
    description: Titles of products in the sections 'you may also like' or 'sponsored products'. Combine results from Max 3 sections
